
# ===============================
# ========= DATA SOURCE =========
# ===============================

# Set here configurations for the database connection
spring.datasource.url=jdbc:mysql://localhost:3306/bankofstates?createDatabaseIfNotExist=true

# ===============================
# ======= JPA / HIBERNATE =======
# ===============================
 
# Show or not log for each sql query
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto=update

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# Username and secret
spring.datasource.username=root
spring.datasource.password=Vela2016

# We set our own application port 
server.port=8081

# riyaz.techproed@gmail.com

# we added this to fix user name and password, interview question 
# after first setting these to facilitate till we write SecurityConfiguration class
#Credentials
#spring.security.user.name= admin
#spring.security.user.password=admin